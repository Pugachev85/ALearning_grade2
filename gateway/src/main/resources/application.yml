spring:
  application:
    name: gateway

  config:
    import: "optional:configserver:http://config-server:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway

server:
  port: 8080
